# Vision360 - Autonomous Driving Vision System for TurtleBot3
# Requirements file with pinned versions for optimal reproducibility

# Core Vision and Deep Learning
opencv-python==4.9.0.80
numpy==1.26.4
ultralytics==8.3.232
torch==2.9.1
torchvision==0.24.1

# Configuration
PyYAML==6.0.3

# Scientific Computing
scipy==1.15.3

# Image Processing
Pillow==12.0.0

# Additional Dependencies (automatically installed with above)
# ultralytics-thop==2.0.18  # Installed with ultralytics

# =============================================================================
# ROS 2 Dependencies (Ubuntu 22.04 - Humble)
# =============================================================================
# These are NOT installed via pip, but via apt:
#
# sudo apt update
# sudo apt install -y ros-humble-desktop
# sudo apt install -y ros-humble-cv-bridge
# sudo apt install -y ros-humble-image-transport
# sudo apt install -y ros-humble-camera-info-manager
# sudo apt install -y ros-humble-image-transport-plugins
#
# After installation, source ROS 2:
# source /opt/ros/humble/setup.bash
# (Add this to ~/.bashrc for persistence)

# =============================================================================
# Installation Instructions
# =============================================================================
# 1. Create a virtual environment (recommended):
#    python3 -m venv venv
#    source venv/bin/activate
#
# 2. Install Python dependencies:
#    pip install -r requirements.txt
#
# 3. Install ROS 2 packages (system-wide, see above)
#
# 4. Download YOLO models (first run will auto-download):
#    - YOLOv8n for person detection
#    - Custom traffic sign/light detector (included in models/)
#
# =============================================================================
# System Requirements
# =============================================================================
# - Python: 3.8 or higher
# - Ubuntu: 22.04 LTS (for ROS 2 Humble)
# - RAM: 4GB minimum, 8GB recommended
# - GPU: Optional (CUDA support for faster inference)
#
# =============================================================================
# Tested Configuration
# =============================================================================
# - Python: 3.10.12
# - Ubuntu: 22.04 LTS
# - ROS 2: Humble Hawksbill
# - OpenCV: 4.9.0.80
# - PyTorch: 2.9.1
# - CUDA: Not required (CPU inference works well)
